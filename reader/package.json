{
  "name": "reader",
  "version": "1.0.0",
  "description": "Client-side TTS reader (Kokoro-82M in the browser)",
  "scripts": {
    "build": "esbuild worker.js --bundle --format=esm --minify --outfile=worker.bundle.js && esbuild app.js --bundle --format=esm --outfile=app.bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  },
  "dependencies": {
    "kokoro-js": "^1.2.1",
    "markdown-it": "^14.3.0"
  },
  "private": true
}
